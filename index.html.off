<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title Here</title>
  <link rel="stylesheet" href="css/main.css">
</head>
<body>
  <h1>Hello World</h1>
  <div id="instafeed"></div>
  <button id="load-more">load more</button>

  <script src="js/j.js"></script>

  <script type="text/javascript">


    // $(document).ready(function() {
       //var loadButton = document.getElementById('load-more');
        var feed = new Instafeed({
        // every time we load more, run this function
            get: 'user',
            userId: 2215020551,
            resolution: 'standard_resolution',
            //clientId: '8d5a58bdf1fe4192bbcb0220b5303c56',
            accessToken: '2215020551.8d5a58b.5ae9e98f7d3b4c6ca984701af05fb260',
            template: '<div><a href="{{link}}" target="_blank"><img src="{{image}}"/></a><p>{{caption}}</p></div>',
          // after: function() {
          //     // disable button if no more results to load
          //     if (!this.hasNext()) {
          //         loadButton.setAttribute('disabled', 'disabled');
          //     }
          //   },
          });

          feed.run();
          // });

          // bind the load more button
          // loadButton.addEventListener('click', function() {
          //     feed.next();
          // });

          $('#load-more').on('click', function() {
           $("#instafeed").fadeTo(400, 0, function() {
            //  $(this).empty(); // remove this line if you want to keep previously loaded images on the page
              userFeed.next();
           })
          });
    // });


    // run our feed!

  </script>

</body>
</html>
